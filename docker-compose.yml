version: "3"
services:
  docker-shell:
    build: .
    image: hdknsmr/dind:1.24
    container_name: docker-shell
    stdin_open: true
    tty: true
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "/root/mnt:/root/mnt"

